import React from 'react'
import { useAppSelector } from '../hooks/redux'

const FavoritesPage = () => {
  const {faovites} = useAppSelector(this.state.)

  return (
    <div>
     
    </div>
  )
}

export default FavoritesPage

