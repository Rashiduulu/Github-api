import React from 'react'
import { useAppSelector } from '../hooks/redux'

const FavoritesPage = () => {
  const {faovites} = useAppSelector

  return (
    <div>
     
    </div>
  )
}

export default FavoritesPage

